name: Unity PlayMode Tests (disabled)

# Workflow has been moved here because GitHub-hosted runners require a Unity license.
# To re-enable: move this file back to `.github/workflows/` and set up a license or self-hosted runner.

# Original workflow preserved for reference.
